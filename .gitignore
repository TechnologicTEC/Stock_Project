.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.DS_Store

# Secrets - never commit your real keys
.env

# Local database and cache files - each collaborator runs their own
# (see Section 13: SQLite gets awkward with two people editing it)
db/*.db
db/*.db-journal
*.sqlite3
