# "Honest terminal" theme — dark blue-ink ground with a single amber accent
# (terminal heritage, deliberately not the near-black + acid-green cliché).
# This sets Streamlit's base tokens; app/_theme.py layers the rest via CSS
# (monospace data, panel-style metric cards, sidebar nav, badges, etc).
[theme]
base = "dark"
primaryColor = "#e8b24a"              # amber — the one bold hue (buttons, focus, accents)
# Grey-blue, not near-black: the first cut (#0a0e14) read as harsh and pushed
# muted text to a 3.65 contrast ratio, under the WCAG AA floor of 4.5. Every tone
# clears AA on this ground — see the note in app/_theme.py.
backgroundColor = "#161b26"
secondaryBackgroundColor = "#1e2531"  # panels, sidebar, inputs
textColor = "#dde5ef"
linkColor = "#e8b24a"
borderColor = "#2c3546"               # hairline
baseRadius = "0.45rem"
font = "sans-serif"                   # system UI sans; data is forced to monospace in CSS
